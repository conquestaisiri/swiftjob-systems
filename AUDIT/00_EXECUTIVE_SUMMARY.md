# SwiftJob production-readiness program

Status: implementation, isolated regression, production deployment, live API smoke checks, responsive browser checks, accessibility DOM checks, dynamic sitemap verification, static security headers, route-level bundle splitting, domain migration, and a designated mailbox logo/delivery check are complete. The final gate remains NOT READY until historical credential rotation, Core Web Vitals/assistive-technology measurement, distributed Cloudflare rate-limit rules, and inbound mailboxes for the new domain are evidenced.

Active goal: complete the owner's eight-phase full-system audit, controlled repairs and connected verification. See `25_IMPLEMENTATION_PLAN.md` for phase gates.

The 2026-09-08 inspection found application dropdown labels missing, homepage candidate access pointing to careers, a generic LinkedIn destination, and singular date wording. These are historical observations to revalidate against the current baseline.

Production domain: https://swiftjob.online. The previous hostname remains an active compatibility alias while the new domain is adopted.

The repair branch contains the controlled API, data-boundary, email, UX, infrastructure, and domain-migration fixes recorded in `26_IMPLEMENTATION_LOG.md`. The additive application idempotency migration was applied without changing existing counts. Request-time schema self-healing is retired; a read-only startup guard now fails clearly when migrations are incomplete. The repaired Worker and Pages build are deployed at `https://swiftjob.online`; the latest Pages deployment is `3374ec21.swiftjob-systems.pages.dev`. The new Pages custom domain and certificate are active, the Worker is deployed with `FRONTEND_URL=https://swiftjob.online`, and the verified Resend domain has sending enabled. The new HR route still needs an inbound mailbox or Email Routing rule. Hosted CI remains configured to use the migrated domain secrets. Closure evidence is in `evidence/provider-access-closure.json`, `evidence/hosted-ci-closure.json`, `evidence/email-dns-closure.json`, `evidence/live-email-flow-closure.json`, `evidence/live-application-email-closure.json`, `evidence/live-contact-email-closure.json`, `evidence/live-admin-mail-closure.json`, `evidence/history-secrets.json`, `evidence/rate-limit-analysis.json`, `evidence/schema-closure.json`, `evidence/accessibility-closure.json`, `evidence/contrast-closure.json`, `evidence/performance-closure.json`, `evidence/responsive-browser-errors.json`, `evidence/access-key-verification.json`, `evidence/branding-closure.json`, and `evidence/deep-qa-closure.json`.
