# Legacy StrixJob remnants

The retired Express artifact at `artifacts/api-server` is not in the active request path. The active architecture is the Pages SPA plus Hono Worker. The repository inventory and history scan retain legacy code and credential-shaped historical objects; they are documented for cleanup and rotation rather than silently deleted during this repair.

No legacy route or brand was reintroduced in the repaired public flows. A separate decommission plan should remove retired runtime dependencies after deployment rollback requirements are confirmed.
