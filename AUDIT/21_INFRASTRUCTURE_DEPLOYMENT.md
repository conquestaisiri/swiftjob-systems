# Infrastructure and deployment

The active deployment is Cloudflare Pages (`swiftjob-systems`) with a Pages Function proxying to the Hono Worker (`swiftjob-workers-api`), Neon PostgreSQL, R2, and Resend. The former private B2/combined MSI path is retired. Provider inventory confirmed access and recorded deployment metadata without emitting secrets.

CI now installs the pinned pnpm version explicitly, uses boolean `allowBuilds` values for `sharp` and `workerd`, runs frozen installs, executes isolated regressions with their standalone runtime dependencies, and fails when a required Worker secret is absent. Hosted CI run `34701538185` passes typecheck, isolated regressions and the production frontend build; production deploy jobs remain gated to `main`. Worker and Pages deploy jobs remain separate. The active Worker version is `002c32fd-b0c6-4f8f-aaae-160e07253c18`; the latest Pages production deployment is `e30419db.swiftjob-systems.pages.dev` under project `swiftjob-systems`. The existing custom domain `https://swiftjob.payservice.top` is live.

Post-deploy checks: `/api/healthz` and `/api/jobs` return 200 with security headers and `no-store`; protected candidate/admin routes reject anonymous access; invalid assessment references return 404; the retired MSI endpoint returns 410; the Pages preflight reflects the same-origin and allows `Idempotency-Key`; dynamic `/sitemap.xml` returns XML with 100 live public-job URLs and `no-store`; five routes at six viewport widths have zero body overflow and zero browser console errors. Migration `013_application_idempotency.sql` was applied additively and existing job/application counts were unchanged.
