import { SidebarAds } from "@/components/site/SidebarAds";

/**
 * Floating site engagement widget — landing page only.
 * A single ad pop-up that closes and reopens at different spots.
 */
export function SiteWidgets() {
  return (
    <>
      <SidebarAds />
    </>
  );
}
